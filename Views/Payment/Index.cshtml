@using PIPMUNI_ARG.Models.Domain;
@model IEnumerable<PIPMUNI_ARG.Models.Domain.Payment>?
@{
    ViewData["Title"] = "Certificados";
    ViewBag.WorkingInContract = true;
}



<form asp-action="Index" asp-controller="Payment" method="get" asp-route-returnUrl="@ViewBag.returnUrl" asp-route-bufferedUrl="@ViewBag.bufferedUrl" >

    <!-- Search -->
    <div class="row mb-4">
        @if ((Model?.Count() > ProjectGlobals.maxRowsBeforeSearch) || (!string.IsNullOrEmpty(@ViewBag.searchText)))
        {
            <div class="col-xl-3 col-lg-2 col-md-1">
            </div>
            <div class="col-xl-9 col-lg-10 col-md-11 col-sm-12">
                <div class="input-group">
                    <input name="searchText" type="search" value="@ViewBag.searchText" class="form-control" />
                   <button type="submit" class="btn btn-outline-secondary"><i class="bi-search"></i><span class="screenOnly">&nbsp; Buscar</span></button>
                </div>
            </div>
        }
    </div>

    <a asp-area="" asp-controller="Payment"  asp-action="Create" asp-route-contractId=@ViewBag.contractId class="btn btn-clear float-end" asp-route-bufferedUrl=@ViewBag.returnUrl><i class="bi-plus"></i> Crear  Certificado</a>

    <!-- select table -->
    <table class="table select-table">
        <thead>
            <tr>
                <th>
                    <a href=@Url.Action("Index", "Payment", new { sortOrder = @ViewBag.codeSort})>
                        @Html.DisplayNameForInnerType((Payment s) => s.Code) <i class=@ViewBag.codeIcon></i>
                    </a>
                </th>
                <th>
                    <a href=@Url.Action("Index", "Payment", new { sortOrder = @ViewBag.typeSort})>
                        @Html.DisplayNameForInnerType((Payment s) => s.Type) <i class=@ViewBag.typeIcon></i>
                    </a>
                </th>
                <th>
                    <a href=@Url.Action("Index", "Payment", new { sortOrder = @ViewBag.totalSort})>
                        @Html.DisplayNameForInnerType((Payment s) => s.Total) <i class=@ViewBag.totalIcon></i>
                    </a>
                </th>
                <th>
                    <a href=@Url.Action("Index", "Payment", new { sortOrder = @ViewBag.dateDeliverySort})>
                        @Html.DisplayNameForInnerType((Payment s) => s.DateDelivery) <i class=@ViewBag.dateDeliveryIcon></i>
                    </a>
                </th>
                <th>
                    <a href=@Url.Action("Index", "Payment", new { sortOrder = @ViewBag.stageSort})>
                        @Html.DisplayNameForInnerType((Payment s) => s.Stage) <i class=@ViewBag.stageIcon></i>
                    </a>
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @if (Model?.Count() > 0)
            {
                @foreach (var payment in Model)
                {
                    <tr>
                        <td class="sel-row">
                            <a asp-action="Edit" asp-route-id="@payment.Id" asp-route-contractId=@ViewBag.contractId asp-route-bufferedUrl=@ViewBag.returnUrl>
                                @Html.DisplayFor(modelItem => payment.Code)
                            </a>
                        </td>
                        <td class="sel-row">
                            <a asp-action="Edit" asp-route-id="@payment.Id" asp-route-contractId=@ViewBag.contractId asp-route-bufferedUrl=@ViewBag.returnUrl>
                                @if (payment.Type_info != null) @Html.DisplayFor(modelItem => payment.Type_info.Name)
                            </a>
                        </td>
                        <td class="sel-row">
                            <a asp-action="Edit" asp-route-id="@payment.Id" asp-route-contractId=@ViewBag.contractId asp-route-bufferedUrl=@ViewBag.returnUrl>
                                @Html.DisplayFor(modelItem => payment.Total)
                            </a>
                        </td>
                        <td class="sel-row">
                            <a asp-action="Edit" asp-route-id="@payment.Id" asp-route-contractId=@ViewBag.contractId asp-route-bufferedUrl=@ViewBag.returnUrl>
                                @Html.DisplayFor(modelItem => payment.DateDelivery)
                            </a>
                        </td>
                        <td class="sel-row">
                            <a asp-action="Edit" asp-route-id="@payment.Id" asp-route-contractId=@ViewBag.contractId asp-route-bufferedUrl=@ViewBag.returnUrl>
                                @if (payment.Stage_info != null) @Html.DisplayFor(modelItem => payment.Stage_info.Name)
                            </a>
                        </td>
                        <td class="sel-row">
                            <a asp-action="Edit" asp-route-id="@payment.Id" asp-route-contractId=@ViewBag.contractId asp-route-bufferedUrl=@ViewBag.returnUrl class="btn nav-link">
                                <i class="bi-pencil"></i>
                            </a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>

</form>
@{
    <!-- return button -->
    if (!string.IsNullOrEmpty(ViewBag.returnUrl))
    {
        string returnlink = @ViewBag.returnUrl;
        if (!string.IsNullOrEmpty(ViewBag.bufferedUrl))
        {
            if (!returnlink.Contains("returnUrl"))
            {
                string separator = (returnlink.Contains('?')) ? "&" : "?";
                returnlink += string.Format("{0}returnUrl={1}", separator, ViewBag.bufferedUrl);
            }
        }
        <a href=@returnlink formnovalidate class="btn btn-basic"><i class="bi-arrow-90deg-left"></i>&nbsp; Regresar</a>
    }
}

@await Html.PartialAsync("/Views/Shared/JaosLib/_ResultMessages.cshtml",ViewData)
